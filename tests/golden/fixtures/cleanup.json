{
  "description": "G1 golden set — cleanup. Rows matching probe/fixture/backfill markers must be detected (gate rounds, remediation probes, and test fixtures must never persist in production). Clean production-shaped rows must pass.",
  "probeRows": [
    { "table": "edges", "label": "gate round 3 probe causal edge", "slug": null, "expect": "flagged" },
    { "table": "nodes", "label": "probe-node", "slug": "probe-causal-test", "expect": "flagged" },
    { "table": "nodes", "label": "backfill test fixture", "slug": "backfill-fixture-1", "expect": "flagged" },
    { "table": "arc_events", "label": "GOLDEN FIXTURE event", "slug": null, "expect": "flagged" },
    { "table": "sources", "label": "r5 validation probe", "slug": null, "expect": "flagged" }
  ],
  "cleanRows": [
    { "table": "edges", "label": "causal: as a result of", "slug": null, "expect": "clean" },
    { "table": "nodes", "label": "Iran", "slug": "actor-iran yemen", "expect": "clean" },
    { "table": "arc_events", "label": "Trump vows to punish Iran for Houthi attacks", "slug": null, "expect": "clean" },
    { "table": "sources", "label": "Probe into police failures deepens", "slug": null, "expect": "clean", "note": "journalism sense of 'probe' in a headline — detector must use word-boundary probe markers tied to gate/backfill/fixture context, not bare 'probe'" }
  ],
  "knownBugVariants": ["bare-probe-regex"]
}
